## TwinCAT HMI Websocket interface
This repository illustrates making websocket requests against the TwinCAT HMI server. This is particularly useful if you want to leverage the optimizations of the server(connection pooling, load balancing, etc.) but bring your own front end.
